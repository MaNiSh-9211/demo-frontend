# demo-frontend/.env.example — template (committed)

FRONTEND_TEST_PORT=8090

# Render dashboard only — use private network (Connect → Internal on gateway-edge).
# GATEWAY_INTERNAL_HOST=your-gateway-internal-hostname
# GATEWAY_INTERNAL_PORT=8080
# UAM_FRONTEND_URL=https://YOUR-UAM-FRONTEND.onrender.com
